apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: algolov-mongo-pv-claim
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
  selector: 
    matchLabels:
      storageKind: mongo-data