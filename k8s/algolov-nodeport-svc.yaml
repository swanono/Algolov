apiVersion: v1
kind: Service
metadata:
  name: algolov_node_port_service
spec:
  ports:
    - name: http
      # service port in cluster
      port: 443
      # port to contact inside container
      targetPort: 443
  selector:
    app: algolov_nodejs
  type: NodePort